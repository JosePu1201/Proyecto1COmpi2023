package com.example.boxworld.Mov

class Aprox {
    fun main() {
        var arregloBi = arrayOf(intArrayOf(1, 2, 3), intArrayOf(4, 5, 6), intArrayOf(7, 8, 9))

        for (row in arregloBi) {
            println(row.contentToString())
        }
    }
}